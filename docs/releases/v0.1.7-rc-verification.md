# AgentShare v0.1.7 Release Candidate Verification

Date: 2026-08-12 Code candidate commit:
`cec21f8b6196530035fb07975b6696a7eda2648c` Status: **BLOCKED - Claude Code
authentication expired**

## Passed Gates

- Fresh `npm ci`: 205 packages audited, 0 vulnerabilities.
- Formatting, TypeScript, ESLint, build, package smoke test, and Worker dry-run.
- Test suite: 66 passed, 5 credential-gated tests skipped.
- Coverage: 69.17% lines, 56.82% branches, 76.99% functions.
- Edge runtime handoff and production relay lifecycle.
- Codex live diagnostic: public create/open/query/revoke/expiry/replay, explicit
  denied file write, denied network request, and grounded two-turn continuity.
- Three adversarial repair cycles. Final independent security and code reviews
  returned `APPROVE` with no code findings.

## Blocking Gate

Claude Code `2.1.210` is installed but its OAuth session is expired. The
Claude-only diagnostic failed both real-agent checks with exit code 1 and
`Failed to authenticate: OAuth session expired and could not be refreshed`.

No v0.1.7 tag, GitHub release, Worker deployment, or browser acceptance test was
performed. This avoids publishing a web command that points at a missing release
asset.

## Resume Procedure

1. Run `claude auth login` and confirm `claude auth status` reports logged in.
2. Set `AGENTSHARE_E2E_RELAY` to the production relay origin.
3. Run `npm run test:release`; both Codex and Claude tests must pass.
4. Push the candidate, wait for GitHub Actions, package and publish v0.1.7,
   deploy the Worker, then run production and in-app-browser acceptance tests.
