# AgentShare v0.1.11 Release Verification

Date: 2026-08-21

Status: **PUBLISHED PUBLIC BETA — READY FOR ANNOUNCEMENT**

Candidate base commit: `e8b7ed4d2dd9cff40d68b1ddb6372a9c35d976ab`

Exact release commit: `63ad80b0a4f2afad3bf66026fff2e4ef0e69df4d`

## Scope

v0.1.11 is a patch release for CLI-managed updates plus the security hardening
merged to `master` after v0.1.10. The relay edge entrypoint and secret scanner
change, while the protocol encryption design and production Durable Object
sequence are preserved, including the active `QueryObject` v3 migration. Active
install and recipient commands, both production Workers, package metadata,
tests, and operations documentation advance to v0.1.11. Historical v0.1.10
evidence remains preserved.

## Blocking release gates

- [x] Exact candidate is clean and frozen.
- [x] Complete local source-quality gate passes with zero unexpected failures.
- [x] Skipped tests are identified and none are skipped in the strict real-agent
      gate.
- [x] Six-job Ubuntu/macOS/Windows × Node 22/24 CI matrix passes on the exact
      candidate.
- [x] Current Codex CLI and Claude Code releases pass compatibility and
      isolation review before allowlisting.
- [x] Production handoff Worker serves the v0.1.11 pinned command with required
      security headers.
- [x] Production relay lifecycle, CORS, limits, replay, revocation, and expiry
      checks pass.
- [x] `npm run test:release` passes with authenticated Codex and Claude, zero
      skipped tests.
- [x] `agentshare-0.1.11.tgz` is built from the exact candidate and its size and
      SHA-256 are recorded.
- [x] Anonymous download matches the recorded artifact digest.
- [x] Fresh isolated install and v0.1.10 → v0.1.11 upgrade pass.
- [x] Public updater checks and installation behavior pass on Windows, macOS,
      and Linux evidence paths.
- [x] One uninterrupted published-package creator → browser → recipient →
      grounded questions → revoke smoke passes. Published CLI required both
      creator approvals, served the trusted handoff, answered two grounded cited
      questions without echoing the capability, revoked through the hidden
      prompt, and returned post-revoke relay metadata 410.
- [x] Rollback targets and commands are recorded.

## Source-quality evidence

| Gate                           | Result | Evidence                                                 |
| ------------------------------ | ------ | -------------------------------------------------------- |
| `npm ci`                       | Pass   | 195 packages installed; 0 vulnerabilities                |
| Repository hygiene             | Pass   | 135 tracked files; no forbidden paths or key material    |
| `npm run format:check`         | Pass   | All matched files use Prettier code style                |
| `npm run lint`                 | Pass   | TypeScript project build and ESLint completed            |
| `npm run build`                | Pass   | TypeScript build and bundled CLI completed               |
| `npm run test:coverage`        | Pass   | 137 passed; 5 expected environment-gated tests skipped   |
| `npm run test:package`         | Pass   | `Packed CLI passed: agentshare-0.1.11.tgz`               |
| `npm run test:edge-runtime`    | Pass   | `Edge runtime handoff passed`                            |
| Relay Wrangler dry-run         | Pass   | Hardened entrypoint; DO and rate-limit bindings resolved |
| Handoff Wrangler dry-run       | Pass   | 9.35 KiB upload; no bindings                             |
| `npm audit --audit-level=high` | Pass   | 0 vulnerabilities                                        |

Skipped-test review: normal coverage intentionally skips four real-host tests
unless `AGENTSHARE_REAL_AGENT_E2E=1`, plus one production relay lifecycle test
unless `AGENTSHARE_E2E_RELAY` and `AGENTSHARE_E2E_HANDOFF` are set. These are
not ignored release coverage: the strict production gate enables both
environments, runs both authenticated hosts, and requires six passed tests with
zero skips.

## Exact-commit CI

| Run       | Commit                                     | Matrix result                     |
| --------- | ------------------------------------------ | --------------------------------- |
| Branch CI | `63ad80b0a4f2afad3bf66026fff2e4ef0e69df4d` | Pass: run `32510969963`, six jobs |
| Master CI | `63ad80b0a4f2afad3bf66026fff2e4ef0e69df4d` | Pass: run `32511796757`, six jobs |

Required jobs: Ubuntu, macOS, and Windows on Node 22 and Node 24. Every job must
run formatting, lint/typechecking, build, coverage, package smoke, edge-runtime
tests, both Worker dry-runs, and high-severity dependency audit.

## Host compatibility and strict production gate

Published versions observed on 2026-08-21:

- Codex CLI: `0.149.0`.
- Claude Code: `2.1.238`.

Installed baseline before review:

- Codex CLI: `0.147.0`, authenticated through ChatGPT.
- Claude Code: `2.1.210`, authenticated through claude.ai.

| Host/version        | Help-contract check | Filesystem isolation | Network isolation    | Grounded two-turn answers | Allowlisted |
| ------------------- | ------------------- | -------------------- | -------------------- | ------------------------- | ----------- |
| Codex CLI 0.149.0   | Pass                | Safe startup refusal | Safe startup refusal | Fail                      | No          |
| Claude Code 2.1.238 | Pass                | Pass                 | Pass                 | Pass                      | Yes         |

Codex 0.149.0 fails safely but is incompatible on reviewed Windows systems: its
new sandbox refuses AgentShare's split filesystem policy rather than running
unsandboxed. Public v0.1.11 therefore continues to fail closed for 0.149.0 and
uses reviewed Codex 0.147.0 for the strict dual-agent release gate.

Strict production origins:

- Relay: `https://agentshare-relay.carnation-vermicelli.workers.dev`
- Handoff: `https://agentshare-handoff.carnation-vermicelli.workers.dev`

`npm run test:release`: pass after exact Worker deployment. Reviewed Codex CLI
0.147.0 and Claude Code 2.1.238 executed all six tests with zero skips. Both
filesystem and network isolation checks, grounded multi-turn answers, citations,
production lifecycle behavior, and fail-closed host checks passed.

## Production Workers

| Worker               | Deployment version                     | Result | Rollback target                        |
| -------------------- | -------------------------------------- | ------ | -------------------------------------- |
| `agentshare-handoff` | `17d8e7e5-b47c-4d40-857f-90e1e3fde006` | Pass   | `2dabe037-5f11-4a36-9eea-83fb9caa4707` |
| `agentshare-relay`   | `b4ee1f37-a050-4fba-a666-db647f89feab` | Pass   | `41ba0841-0d44-45b3-8685-8cc4368233f5` |

The relay must deploy the hardened `secure-worker.ts` entrypoint after exact
candidate CI. Published `ShareObject` v1, `RelayControl` v2, and `QueryObject`
v3 migrations remain intact. Rollback must never delete or recreate any Durable
Object namespace.

## Artifact and publication

| Item                        | Evidence                                                                  |
| --------------------------- | ------------------------------------------------------------------------- |
| Tag                         | Annotated `v0.1.11`                                                       |
| Release commit              | `63ad80b0a4f2afad3bf66026fff2e4ef0e69df4d`                                |
| Asset                       | `agentshare-0.1.11.tgz`                                                   |
| Byte size                   | `103645` bytes                                                            |
| SHA-256                     | `9e0cac0d7fd95fe4fc0499056a148503ef96f595e03cfee3f31877b0d9a1c3a4`        |
| GitHub release URL          | `https://github.com/amazing-aaryan/AgentShare/releases/tag/v0.1.11`       |
| Release state               | Public, non-draft, non-prerelease, immutable                              |
| Anonymous redownload digest | Match: `9e0cac0d7fd95fe4fc0499056a148503ef96f595e03cfee3f31877b0d9a1c3a4` |
| Handoff pinned asset        | Pass: live page contains v0.1.11 and not v0.1.10                          |

## Installation, upgrade, and updater evidence

- Fresh isolated v0.1.11 installation from the anonymous release URL: pass.
- `agentshare --version` reports `0.1.11`: pass.
- Normal usage, `init`, `repair`, and `remove`: pass.
- Unmanaged integration conflict preservation: pass in package smoke.
- Official v0.1.10 → v0.1.11 supported manual install upgrade: pass; npm
  reported 0.1.10 before replacement and 0.1.11 afterward, and integrations
  repaired.
- v0.1.10 predates the `update` command, so it cannot self-update itself. The
  first live installation path exercised by the updater can only be tested when
  a newer stable release exists. This limitation is now explicit evidence, not
  treated as a successful v0.1.10 self-update.
- Existing protocol links remain compatible because v0.1.11 preserves ACB v1 and
  the deployed relay migrations; automated compatibility coverage passes.
- `agentshare update --check` identifies v0.1.11 as current: pass.
- Passive 24-hour cache, opt-out, no-install behavior, and failure isolation:
  pass in updater unit and CLI tests.
- Windows/macOS/Linux updater behavior: pass through the exact six-job CI
  matrix, including Windows-specific npm discovery tests. A live forward install
  remains impossible until v0.1.12 or later exists.

## Published-package production smoke

Observed using the anonymously downloaded release asset and a real pseudo-TTY:

1. Public asset install, `npm exec`, and initialization passed.
2. Published CLI displayed the normalized review and required both approvals.
3. Trusted handoff page returned 200, pinned v0.1.11, and relay metadata
   returned 200 before recipient use.
4. Published recipient CLI accepted the capability through its hidden prompt;
   the capability was not echoed.
5. Two Codex questions returned grounded `ORBIT` and deterministic-deployment
   answers with bundle citations.
6. Published CLI accepted the same capability through the hidden revoke prompt;
   relay metadata returned 410 afterward. The uninterrupted harness reported two
   creator approvals, handoff status 200, two grounded cited questions, no
   capability echo, successful published CLI revocation, and post-revoke 410.

## Security, privacy, and beta position

- Public-beta warning remains required.
- Do not claim production-grade guarantees.
- Do not send production credentials, regulated data, or other high-risk data.
- Secret scanning remains best-effort.
- Bearer-link forwarding, trusted handoff-origin compromise, endpoint
  compromise, and model-provider exposure of selected excerpts remain explicit
  residual risks.
- Citations establish bundle provenance, not external truth.
- The updater trusts GitHub release metadata and assets. In-client checksum or
  signature verification remains post-beta hardening, while every release must
  publish and independently verify a SHA-256 digest.

## Rollback

- Primary package rollback: v0.1.10 release asset.
- Handoff rollback: deployment `2dabe037-5f11-4a36-9eea-83fb9caa4707`.
- Relay rollback: deployment `41ba0841-0d44-45b3-8685-8cc4368233f5`; preserve
  all Durable Object storage and migrations. Promotion rollback: stop
  advertising v0.1.11, keep the immutable evidence, and direct users to the
  v0.1.10 asset while investigating. Never replace a published asset in place.

## Final decision

**Ready for public-beta announcement.** The immutable v0.1.11 release is public,
and all exact-source, CI, Worker, strict real-agent, artifact, digest,
fresh-install, upgrade, and uninterrupted published-package gates passed. The
published flow confirmed two creator approvals, trusted handoff status 200, two
grounded cited recipient turns, hidden capability handling, CLI revocation, and
post-revoke 410. No source or production rollback is currently indicated.
