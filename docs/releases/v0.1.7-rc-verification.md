# AgentShare v0.1.7 Release Candidate Verification

Date: 2026-08-12 Code candidate commit:
`cec21f8b6196530035fb07975b6696a7eda2648c` Status: **PASSED**

## Passed Gates

- Fresh `npm ci`: 205 packages audited, 0 vulnerabilities.
- Formatting, TypeScript, ESLint, build, package smoke test, and Worker dry-run.
- Test suite: 66 passed, 5 credential-gated tests skipped.
- Coverage: 69.17% lines, 56.82% branches, 76.99% functions.
- Edge runtime handoff and production relay lifecycle.
- Strict `npm run test:release`: 6 passed, 0 skipped against the production
  relay with Codex CLI 0.145.0 and Claude Code 2.1.210.
- Both agents attempted benign file/network operations; launcher isolation
  prevented filesystem and network side effects.
- Both agents preserved grounded two-turn continuity with citations.
- Three adversarial repair cycles. Final independent security and code reviews
  returned `APPROVE` with no code findings.

## Authentication Retest

The initial Claude diagnostic failed because its OAuth session had expired.
After re-authentication, `claude auth status` reported `loggedIn: true` and the
strict cross-agent gate passed all six tests.

## Publication Sequence

Push candidate, wait for GitHub Actions, package and publish v0.1.7, deploy the
Worker, then run production and in-app-browser acceptance tests.
