# Sessions and terminal lifecycle

## Objective and dependencies

Implement this approved boundary; dependencies: 03. Read index and repository
AGENTS/reasoning first. Preserve unrelated work. No commits/deploys merely from
completing this PR.

## File map

Codex adapter, CLI share/inbox/recovery, integration templates

## Implementation and contracts

Exact host or explicit session identity, configured storage, metadata
validation; never choose latest. Separate project root and conversation;
explicit relocation reviewed, no transcript rewrite. Same-draft paginated
terminal review, cancel default, no --yes. Existing environment chooser, saved
relay/policy, management without capture, scoped inbox/recovery, truthful
pending states.

## Tests and acceptance

Ambiguous/missing sessions, both project copies, inaccessible roots, duplicate
shares, pending link and scoped recovery.

Record focused tests, full integration results, limitations, and exact changed
files. No success claim from test presence alone.

## Compatibility, rollout and rollback

Legacy commands wrap draft service; never restore capture race.

## Non-goals

Other PRs own their listed boundaries; no credentials/publication/release
changes outside explicit verified workflow.
