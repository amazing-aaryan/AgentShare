# AgentShare v0.2.0 Release Verification

- Status: **release candidate; not yet promoted to stable**
- Verification date: 2026-08-23
- Candidate merge commit: `fee3a771d0afded88f3a04c5b64d686e1296ee65`
- Implementation PR: #6 (`agent/20260822-roadmap-completion`)
- Package version: `0.2.0`

This record tracks the evidence required by
[`../operations/cloudflare-deployment.md`](../operations/cloudflare-deployment.md).
A checked repository gate is not, by itself, permission to publish the stable
release. The immutable package, live split-origin deployment, and authenticated
real Codex/Claude isolation gate must all be recorded before promotion.

## Repository Integration

PR #6 was merged into `master` on 2026-08-23 as
`fee3a771d0afded88f3a04c5b64d686e1296ee65` with the verified feature head
`2cb708c56078bcc7bb8e2fa07b0d6dd370c34b03` pinned at merge time.

The last PR matrix for the feature head passed on Ubuntu, macOS, and Windows with
Node.js 22 and 24. Each job passed repository hygiene, Prettier, TypeScript/ESLint,
build, coverage, the ACB v1 conformance gate, packed-package tests, edge-runtime
tests, both Wrangler dry-runs, and `npm audit --audit-level=high`.

The Linux/Node 24 reference job reported:

- 55 test files passed and 2 live/release files skipped;
- 228 tests passed and 6 live/release tests skipped;
- ACB v1 conformance passed independently;
- packed CLI verification passed for `agentshare-0.2.0.tgz`;
- edge runtime handoff passed;
- relay and handoff Wrangler dry-runs passed;
- `npm audit` found 0 vulnerabilities.

Immediately before the merge, `master` had advanced by one commit from the base
used by that PR matrix. The intervening change modified only `LICENSE` (one line
replaced). This release-verification branch was therefore cut from the actual
post-merge `master` state to force a fresh full PR matrix against the merged tree.
The result of that matrix must be recorded below before release staging proceeds.

### Fresh merged-tree matrix

- [ ] Ubuntu / Node.js 22
- [ ] Ubuntu / Node.js 24
- [ ] macOS / Node.js 22
- [ ] macOS / Node.js 24
- [ ] Windows / Node.js 22
- [ ] Windows / Node.js 24

## Immutable Package Staging

Required before deploying the v0.2 handoff Worker because the Worker pins this
exact asset:

```text
https://github.com/amazing-aaryan/AgentShare/releases/download/v0.2.0/agentshare-0.2.0.tgz
```

- [ ] Create/stage GitHub release `v0.2.0` as a **prerelease**, not stable.
- [ ] Build the package from the exact verified candidate commit.
- [ ] Upload `agentshare-0.2.0.tgz` once.
- [ ] Record byte length.
- [ ] Record SHA-256.
- [ ] Verify an anonymous download matches the recorded byte length and SHA-256.
- [ ] Perform a fresh isolated installation from that exact asset.

Package byte length: **pending**

Package SHA-256: **pending**

## Production Deployment

Official origins:

```text
https://agentshare-relay.carnation-vermicelli.workers.dev
https://agentshare-handoff.carnation-vermicelli.workers.dev
```

Required deployment order and evidence:

- [ ] Deploy the relay while preserving Durable Object migrations `v1` through
      `v4`.
- [ ] Deploy the independent handoff Worker after the immutable v0.2.0 package
      asset exists.
- [ ] Verify `/s/` handoff security headers and legacy flow.
- [ ] Verify `/e/` environment page security headers and split-origin behavior.
- [ ] Verify `/e/<id>/bootstrap.json` advertises v0.2.0 and the exact immutable
      package URL.
- [ ] Verify one disposable live v1 share.
- [ ] Verify one disposable live v2 environment.

Relay deployment version: **pending**

Handoff deployment version: **pending**

## Strict Live Release Gate

From the exact deployed release candidate:

```powershell
$env:AGENTSHARE_E2E_RELAY="https://agentshare-relay.carnation-vermicelli.workers.dev"
$env:AGENTSHARE_E2E_HANDOFF="https://agentshare-handoff.carnation-vermicelli.workers.dev"
npm run test:release
```

The gate must run with authenticated reviewed Codex and Claude installations and
must not be reduced to a one-agent diagnostic.

- [ ] Distinct HTTPS relay/handoff origins verified.
- [ ] Real public v1 handoff lifecycle verified.
- [ ] Real public v2 bootstrap/revision retrieval verified.
- [ ] Codex filesystem and network isolation verified on a reviewed exact
      version.
- [ ] Claude filesystem and network isolation verified on a reviewed exact
      version.

Codex version/evidence: **pending**

Claude version/evidence: **pending**

Strict gate result: **pending**

## Stable Promotion Gate

Do **not** mark `v0.2.0` stable until every item above is complete.

After all release gates pass:

- [ ] Promote the staged `v0.2.0` prerelease to stable without replacing the
      verified tarball.
- [ ] Re-verify anonymous asset download and checksum.
- [ ] Confirm the trusted handoff Worker still pins the same asset.
- [ ] Record the final release URL and deployment identifiers here.

Final status: **NOT YET RELEASED AS STABLE**
