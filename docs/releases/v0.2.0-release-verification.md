# AgentShare v0.2.0 Release Verification

- Status: **immutable prerelease staged; live deployment blocked on Cloudflare credentials**
- Verification date: 2026-08-23
- Final release-candidate commit: `4a570062806048057fdefd5c5aa27d26923b684a`
- Implementation PR: #6 (`agent/20260822-roadmap-completion`)
- Release-verification PR: #7 (`release/v0.2.0-verification`)
- Live operational gate PR: #8 (`release/v0.2.0-live`)
- Package version: `0.2.0`

This record tracks the evidence required by
[`../operations/cloudflare-deployment.md`](../operations/cloudflare-deployment.md).
A checked repository or package gate is not, by itself, permission to promote
the stable release. The live split-origin deployment and authenticated real
Codex/Claude isolation gate must still be recorded before promotion.

## Repository Integration

PR #6 was merged into `master` on 2026-08-23 as
`fee3a771d0afded88f3a04c5b64d686e1296ee65` with the verified feature head
`2cb708c56078bcc7bb8e2fa07b0d6dd370c34b03` pinned at merge time.

Immediately before that merge, `master` had advanced by one commit from the base
used by the final implementation PR matrix. The intervening change modified only
`LICENSE`. A fresh merged-tree package gate correctly detected that
`packages/cli/LICENSE` had not been synchronized with the new root license. PR #7
synchronized the packaged license without weakening the package test and was
then re-run through the complete matrix.

Fresh release-candidate matrix run: GitHub Actions `32645020435`.

### Fresh merged-tree matrix

- [x] Ubuntu / Node.js 22
- [x] Ubuntu / Node.js 24
- [x] macOS / Node.js 22
- [x] macOS / Node.js 24
- [x] Windows / Node.js 22
- [x] Windows / Node.js 24

Every job passed repository hygiene, Prettier, TypeScript/ESLint, build,
coverage, the ACB v1 conformance gate, packed-package tests, edge-runtime tests,
both Wrangler dry-runs, and `npm audit --audit-level=high`.

The Linux reference runs reported 55 test files passing, 228 tests passing, and
6 intentionally skipped live/authenticated release tests. The dedicated ACB v1
conformance test passed, the packed CLI passed, both Worker dry-runs passed, and
`npm audit` found 0 vulnerabilities.

PR #7 was then merged into `master` as the final release-candidate commit
`4a570062806048057fdefd5c5aa27d26923b684a`.

## Immutable Package Staging

The handoff Worker pins this exact asset:

```text
https://github.com/amazing-aaryan/AgentShare/releases/download/v0.2.0/agentshare-0.2.0.tgz
```

A one-shot same-repository GitHub Actions staging workflow checked out the exact
release-candidate SHA above, built the package, ran the package verifier, refused
to replace a pre-existing release, created `v0.2.0` as a prerelease, verified its
metadata, anonymously re-downloaded the asset, compared checksum and byte length,
performed an isolated installation, and deleted its temporary staging branch only
after success.

PR #8 independently re-verified the staged release metadata and asset before its
Cloudflare credential gate.

- [x] Create/stage GitHub release `v0.2.0` as a **prerelease**, not stable.
- [x] Build the package from the exact verified candidate commit.
- [x] Upload `agentshare-0.2.0.tgz` once.
- [x] Record byte length.
- [x] Record SHA-256.
- [x] Verify an anonymous download matches the recorded byte length and SHA-256.
- [x] Perform a fresh isolated installation from that exact asset.

Release URL:
`https://github.com/amazing-aaryan/AgentShare/releases/tag/v0.2.0`

Release target: `4a570062806048057fdefd5c5aa27d26923b684a`

Package byte length: **131825 bytes**

Package SHA-256:
**`ab8033629c919ea49eb2aa0c4966ae61364fc770832d9174a22701381d175cd7`**

Isolated install version check: **`0.2.0`**

## Production Deployment

Official origins:

```text
https://agentshare-relay.carnation-vermicelli.workers.dev
https://agentshare-handoff.carnation-vermicelli.workers.dev
```

PR #8 contains the operational-only live gate. GitHub Actions run `32645386277`
re-verified the prerelease, target SHA, package SHA-256, byte length, and isolated
install successfully. It then failed at the explicit pre-write credential check
because the repository currently has neither `CLOUDFLARE_API_TOKEN` nor
`CLOUDFLARE_ACCOUNT_ID` configured. The relay and handoff deploy steps were
therefore skipped and **no production Cloudflare write occurred**.

Once those repository secrets are configured, re-running the failed live-gate
job in PR #8 is the prepared deployment path.

Required deployment order and evidence:

- [ ] Deploy the relay while preserving Durable Object migrations `v1` through
      `v4`.
- [ ] Deploy the independent handoff Worker after the immutable v0.2.0 package
      asset exists.
- [ ] Verify `/s/` handoff security headers and legacy flow.
- [ ] Verify `/e/` environment page security headers and split-origin behavior.
- [ ] Verify `/e/<id>/bootstrap.json` advertises v0.2.0 and the exact immutable
      package URL.
- [ ] Verify one disposable live v1 share.
- [ ] Verify one disposable live v2 environment.

Relay deployment version: **pending Cloudflare credentials**

Handoff deployment version: **pending Cloudflare credentials**

## Strict Live Release Gate

From the exact deployed release candidate:

```powershell
$env:AGENTSHARE_E2E_RELAY="https://agentshare-relay.carnation-vermicelli.workers.dev"
$env:AGENTSHARE_E2E_HANDOFF="https://agentshare-handoff.carnation-vermicelli.workers.dev"
npm run test:release
```

The gate must run with authenticated reviewed Codex and Claude installations and
must not be reduced to a one-agent diagnostic. It cannot be completed before the
v0.2.0 Workers are deployed.

- [ ] Distinct HTTPS relay/handoff origins verified.
- [ ] Real public v1 handoff lifecycle verified.
- [ ] Real public v2 bootstrap/revision retrieval verified.
- [ ] Codex filesystem and network isolation verified on a reviewed exact
      version.
- [ ] Claude filesystem and network isolation verified on a reviewed exact
      version.

Codex version/evidence: **pending live deployment and authenticated host**

Claude version/evidence: **pending live deployment and authenticated host**

Strict gate result: **pending**

## Stable Promotion Gate

Do **not** mark `v0.2.0` stable until every live deployment and strict release
gate item above is complete.

After all release gates pass:

- [ ] Promote the staged `v0.2.0` prerelease to stable without replacing the
      verified tarball.
- [ ] Re-verify anonymous asset download and checksum.
- [ ] Confirm the trusted handoff Worker still pins the same asset.
- [ ] Record the final release URL and deployment identifiers here.

Final status: **PRERELEASE STAGED; NOT YET RELEASED AS STABLE**
