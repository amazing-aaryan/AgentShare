name: format-doc-debug

on:
  push:
    branches:
      - agent/codex-min-version-compat

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx prettier docs/recipient-compatibility.md --write
      - run: git diff -- docs/recipient-compatibility.md
