# v0.3.0 rollout

## Objective and dependencies

Implement this approved boundary; dependencies: 07. Read index and repository
AGENTS/reasoning first. Preserve unrelated work. No commits/deploys merely from
completing this PR.

## File map

CLI/package metadata, bootstrap/handoff pins, integration/support docs

## Implementation and contracts

Build once immutable candidate, verify anonymous download/digest/install, run07
exact bytes. Promote same bytes only on all passes, Windows/Codex-only. Preserve
v0.2.0 and Worker migration history. Test stable/v0.2 upgrades and pending
state. Update local CLI/skills after verification; no unverified current-session
claim.

## Tests and acceptance

Exact artifact, upgrade, state/link preservation, all profile cases.

Record focused tests, full integration results, limitations, and exact changed
files. No success claim from test presence alone.

## Compatibility, rollout and rollback

Verified compatible Worker/pin rollback or compatible reinstall/forward fix. No
state deletion, auto downgrade or workspace reversal.

## Non-goals

Other PRs own their listed boundaries; no credentials/publication/release
changes outside explicit verified workflow.
